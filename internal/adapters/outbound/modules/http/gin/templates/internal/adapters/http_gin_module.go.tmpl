package adapters

import (
	httpgin "{{ .Module }}/internal/adapters/inbound/http/gin"
)

// init registers the HTTP Gin module into the adapters module so that
// its lifecycle hooks are wired when the app starts.
func init() {
	Register(httpgin.Module())
}
